# How to maintain two database connections in Spring Boot
Here I have used two database connections (MySQL and PostgreSQL). 
Each of them is pointed to the relevant Repository packages.
You just need to use repositories as like single datasource application. But they will be stored in relevant database.
